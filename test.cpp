#include <iostream> // Include the necessary header file

using namespace std; // Declare the namespace

int main() {
    int t = 1;
    cout << t << endl;
    int n = 1e6; // maximum size of the array
    cout << n << endl;
    for (int i = 0; i < n; i++) {
        cout << i << " ";
    }
    cout << endl;
    return 0;
}